import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sistemarh.edumak',
  appName: 'sistema-rh-edumark',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
